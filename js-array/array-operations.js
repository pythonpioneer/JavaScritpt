let arr = [1, 2, 3];

// inserting element in array
arr[4] = 4;
arr.push(5);
console.log(arr);

// deletion in array
arr.pop(1);  // delete last element
arr.pop();  // delete last element
console.log(arr);